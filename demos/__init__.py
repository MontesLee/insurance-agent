"""Phase 11 demo runners — one command each, repeatable, real runtime only.

Every demo drives the REAL runtime (Planner providers, Harness, specialist
agents, MessageBus, Eval, Approval, Control Plane) through the benchmark
machinery and prints status/stage/agent/artifact/eval lines only — never
chain-of-thought.
"""
